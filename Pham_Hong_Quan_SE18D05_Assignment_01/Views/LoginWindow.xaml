<Window x:Class="Pham_Hong_Quan_SE18D05_Assignment_01.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Pham_Hong_Quan_SE18D05_Assignment_01"
        xmlns:converters="clr-namespace:Pham_Hong_Quan_SE18D05_Assignment_01.Converters"
        Title="FU Mini Hotel System - Login" Height="500" Width="600"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FF6B73FF" Offset="0"/>
                <GradientStop Color="#FF9DD5FF" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Border Background="White" CornerRadius="15" Margin="80">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="5" Opacity="0.3"/>
            </Border.Effect>
            <StackPanel Margin="50" VerticalAlignment="Center">
                <TextBlock Text="FU Mini Hotel System" FontSize="28" FontWeight="Bold" 
                           HorizontalAlignment="Center" Margin="0,0,0,40" Foreground="#FF6B73FF"/>

                <TextBlock Text="Email Address:" FontWeight="SemiBold" Margin="0,10,0,5" FontSize="14"/>
                <TextBox x:Name="EmailTextBox" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" 
                         FontSize="14"/>

                <TextBlock Text="Password:" FontWeight="SemiBold" Margin="0,15,0,5" FontSize="14"/>
                <PasswordBox x:Name="PasswordBox" FontSize="14" 
                             PasswordChanged="PasswordBox_PasswordChanged"/>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" 
                           Margin="0,10,0,0" TextWrapping="Wrap" FontSize="12"
                           Visibility="{Binding ErrorMessage, Converter={x:Static converters:StringToVisibilityConverter.Instance}}"/>

                <Button Content="LOGIN" Command="{Binding LoginCommand}" 
                        Background="#FF6B73FF" Foreground="White" FontWeight="Bold"
                        Height="45" Margin="0,25,0,0" FontSize="16"/>

                <StackPanel Margin="0,30,0,0">
                    <TextBlock Text="Test Accounts:" FontWeight="Bold" FontSize="12" 
                               HorizontalAlignment="Center" Foreground="Gray"/>
                    <TextBlock Text="Admin: admin@FUMiniHotelSystem.com / @@abc123@@" 
                               FontSize="11" HorizontalAlignment="Center" Margin="0,5,0,0" 
                               Foreground="Gray"/>
                    <TextBlock Text="Customer: john.doe@email.com / password123" 
                               FontSize="11" HorizontalAlignment="Center" Margin="0,2,0,0" 
                               Foreground="Gray"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>
