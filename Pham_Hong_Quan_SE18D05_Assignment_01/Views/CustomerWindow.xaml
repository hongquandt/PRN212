<Window x:Class="Pham_Hong_Quan_SE18D05_Assignment_01.Views.CustomerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Customer Dashboard - FU Mini Hotel System" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#FF6B73FF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderBrush" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF5555FF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#FF6B73FF" Padding="20,15">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="👤" FontSize="24" Margin="0,0,10,0"/>
                    <TextBlock x:Name="WelcomeText" FontSize="20" FontWeight="Bold" 
                              Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>
                <Button Content="Logout" HorizontalAlignment="Right" Click="Logout_Click"/>
            </Grid>
        </Border>

        <!-- Main Content -->
        <TabControl Grid.Row="1" Margin="20" FontSize="14">
            <TabItem Header="👤 My Profile" FontSize="14">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Row="0" Grid.ColumnSpan="2" Content="Edit Profile" HorizontalAlignment="Right" Click="EditProfile_Click"/>
                    <TextBlock Grid.Row="1" Text="Customer ID:" FontWeight="Bold"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" x:Name="CustomerIdTextBlock"/>
                    <TextBlock Grid.Row="2" Text="Full Name:" FontWeight="Bold"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" x:Name="FullNameTextBlock"/>
                    <TextBlock Grid.Row="3" Text="Email:" FontWeight="Bold"/>
                    <TextBlock Grid.Row="3" Grid.Column="1" x:Name="EmailTextBlock"/>
                    <TextBlock Grid.Row="4" Text="Telephone:" FontWeight="Bold"/>
                    <TextBlock Grid.Row="4" Grid.Column="1" x:Name="TelephoneTextBlock"/>
                    <TextBlock Grid.Row="5" Text="Birthday:" FontWeight="Bold"/>
                    <TextBlock Grid.Row="5" Grid.Column="1" x:Name="BirthdayTextBlock"/>
                </Grid>
            </TabItem>
            <TabItem Header="🏠 Room Booking" FontSize="14">
                <Grid Margin="20">
                    <DataGrid x:Name="RoomsDataGrid" AutoGenerateColumns="False" 
                              IsReadOnly="True" SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding RoomID}" Width="50"/>
                            <DataGridTextColumn Header="Room Number" Binding="{Binding RoomNumber}" Width="120"/>
                            <DataGridTextColumn Header="Description" Binding="{Binding RoomDescription}" Width="250"/>
                            <DataGridTextColumn Header="Capacity" Binding="{Binding RoomMaxCapacity}" Width="100"/>
                            <DataGridTextColumn Header="Price" Binding="{Binding RoomPricePerDate, StringFormat=C}" Width="100"/>
                            <DataGridTextColumn Header="Type" Binding="{Binding RoomType.RoomTypeName}" Width="100"/>
                            <DataGridTemplateColumn Header="Actions" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Book" Click="BookRoom_Click" Tag="{Binding RoomID}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            <TabItem Header="📋 Booking History" FontSize="14">
                <Grid Margin="20">
                    <DataGrid x:Name="BookingsDataGrid" AutoGenerateColumns="False" 
                              IsReadOnly="True" SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Booking ID" Binding="{Binding BookingReservationID}" Width="100"/>
                            <DataGridTextColumn Header="Booking Date" Binding="{Binding BookingDate, StringFormat=dd/MM/yyyy}" Width="150"/>
                            <DataGridTextColumn Header="Total Price" Binding="{Binding TotalPrice, StringFormat=C}" Width="150"/>
                            <DataGridTextColumn Header="Room Number" Binding="{Binding BookingDetails[0].RoomInformation.RoomNumber, TargetNullValue='N/A'}" Width="150"/>
                            <DataGridTextColumn Header="Start Date" Binding="{Binding BookingDetails[0].StartDate, StringFormat=dd/MM/yyyy, TargetNullValue='N/A'}" Width="150"/>
                            <DataGridTextColumn Header="End Date" Binding="{Binding BookingDetails[0].EndDate, StringFormat=dd/MM/yyyy, TargetNullValue='N/A'}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>